\documentclass[tikz, border=0mm]{standalone}

\usetikzlibrary{shapes,arrows,positioning, calc}

\begin{document}

\tikzset{%
    block1/.style={
        draw,
        fill=white, 
        rectangle, 
        minimum height=2em, 
        minimum width=3em,
        node distance=1.2cm
    },
    input/.style={
        inner sep=0pt, 
        node distance=1.2cm
    },      
    output/.style={
        inner sep=0pt, 
        node distance=1.2cm
    },      
    sum/.style = {
        draw, 
        fill=white, 
        circle, 
        minimum size=2mm, 
        inner sep=0pt,
        node distance=0.6cm
    },
    pinstyle/.style = {
        pin edge={to-,thin,black}
    },
    branch/.style={
        fill,
        circle,
        minimum size=3pt,
        outer sep=-1pt,
        inner sep=0pt, 
        node distance=0.6cm
    }
}

\begin{tikzpicture}[auto, >=latex']

    \node[input] (input) {};
    \node [block1, right = of input] (proceso) {$Proceso$};
    \node [output, right = of proceso] (output) {};    

    \draw [draw,->] (input) node[above right] {$x(t)$} -- (proceso);
    \draw [->] (proceso) -- (output) node [name=y, above left] {$y(t)$};

\end{tikzpicture}
\end{document}